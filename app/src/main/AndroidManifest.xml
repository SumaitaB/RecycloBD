<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recyclobd">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PaymentActivity"></activity>
        <activity android:name=".GlassActivity" />
        <activity android:name=".BookActivity" />
        <activity android:name=".OthersProduct" />
        <activity android:name=".MetalActivity" />
        <activity android:name=".PaperActivity" />
        <activity android:name=".ClothsActivity" />
        <activity android:name=".Ewaste_Product" />
        <activity android:name=".MainActivity" />
        <activity android:name=".PlasticProduct" />
        <activity android:name=".SellProduct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>